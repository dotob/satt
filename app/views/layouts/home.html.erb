<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Satt</title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <!-- Le styles -->
    <style>
      body {
        padding-top: 60px; /* 60px to make the container go all the way to the bottom of the topbar */
      }
    </style>
    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Le fav and touch icons -->
    <link href="images/favicon.ico" rel="shortcut icon">
    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body>

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
          <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">Satt</a>
          <div class="container nav-collapse">
          <ul class='nav pull-right'>
          <% if user_signed_in? %>
            <li> <%= link_to current_user.email, '#' %> </li>
            <li> <%= link_to content_tag('span', 'log out', :class => 'label label-important'), destroy_user_session_path, :method => :delete %> </li>
            <li> <%= image_tag "#{(request.ssl? ? 'https://secure' : 'http://www')}.gravatar.com/avatar/#{Digest::MD5.hexdigest current_user.email}?s=30", :style => 'padding-top:5px' %></li>
          <% else %>
            <li> <%= link_to('Login', new_user_session_path)  %>  </li>
          <% end %>
          </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>   
    <div class="container">
      <div class="content">
        <div class="row">
          <div class="span4 offset4">
            <% flash.each do |name, msg| %>
              <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
                <a class="close" data-dismiss="alert">&#215;</a>
                <%= content_tag :div, msg, :id => "flash_#{name}" if msg.is_a?(String) %>
              </div>
            <% end %>
          </div>
        </div>
        
        <div class="row">
          
          <div class="span3">
            <div class="well sidebar-nav">
              <h3>Masterorders</h3>
                <% MasterOrder.all_today_created_master_orders.each do |mo| %>
                    <a class="btn btn-<%=  mo.deadline_crossed ? "danger" : "success" %>" 
                      href="/choose_masterorder/<%= mo.id%>" >
                      <strong><%= mo.menu.name %></strong><br />
                      <%= mo.user.name %>
                    </a>
                <% end %>
                  <% can_create_own_master_order = false && MasterOrder.today_created_master_order_by_user_id(current_user.id).nil? %>
                  <a class= "btn btn-warning <%= can_create_own_master_order ? "" : "disabled" %>" href="<%= can_create_own_master_order ? "/create_master_order" : "#" %>" >
                    <strong>Eigene Masterorder anlegen</strong>
                  </a>
            </div>
            

            <div class="well sidebar-nav">
              <% masterorders = MasterOrder.all_today_created_master_orders_by_user_id(current_user.id) %>
              <a href="/show_userorders_of_masterorder" class="btn btn-info <%= masterorders.empty? ? "disabled" : "" %>">Masteransicht</a>
            </div>
          </div>

          <div class="span9">
            <%= yield %>
          </div>
        </div><!--/row-->

      </div><!--/content-->

      <footer>
        <p>&copy; die praktikanten 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>